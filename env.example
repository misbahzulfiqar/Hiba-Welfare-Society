# Optional: production API origin (include protocol, no trailing slash)
# Leave empty in dev — Vite proxies /api to http://localhost:3001
# VITE_API_URL=https://your-api.example.com

# Set FRONTEND_URL in src/server/.env for Easypaisa return/cancel URLs (e.g. http://localhost:5173)
